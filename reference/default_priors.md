# Default priors' hyperparameters

Returns default priors' hyperparameters for the DRM model.

## Usage

``` r
default_priors()
```

## Value

A `list` containing the default hyperparameter values for the priors.

## Author

lcgodoy
