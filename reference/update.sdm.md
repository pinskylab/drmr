# Update and Re-fit a SDM Model

Update and Re-fit a SDM Model

## Usage

``` r
# S3 method for class 'sdm'
update(object, ...)
```

## Arguments

- object:

  An object of class `sdm`.

- ...:

  Arguments to be updated in the new call.

## Value

An updated `adrm` object or the unevaluated call.
