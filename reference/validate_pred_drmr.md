# Validate a pred_drmr list object

Checks if an object is a valid `pred_drmr` object by ensuring it is a
`list` and contains all required fields.

## Usage

``` r
validate_pred_drmr(x)
```

## Arguments

- x:

  An object to validate.

## Value

Nothing if validation is successful. Throws an error on failure.
