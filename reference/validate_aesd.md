# Validate a aesd list object

Checks if an object is a valid `aesd` object by ensuring it is a `list`
and contains all required fields.

## Usage

``` r
validate_aesd(x)
```

## Arguments

- x:

  An object to validate.

## Value

Nothing if validation is successful. Throws an error on failure.
