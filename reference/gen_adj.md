# Generates an adjacency matrix

Generates an adjacency matrix

## Usage

``` r
gen_adj(x)
```

## Arguments

- x:

  an `sf` object representing the patches.

## Value

An adjacency `matrix`.

## Author

lcgodoy
